#include "pch.h"

namespace foobar {

    TEST(TestCaseName, TestName) {
        EXPECT_EQ(1, 1);

        //char* xxx = (char*)malloc(10);
        //char z = xxx[12];

        EXPECT_TRUE(true);
    }

}
